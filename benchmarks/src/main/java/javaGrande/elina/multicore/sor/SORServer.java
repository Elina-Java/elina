package javaGrande.elina.multicore.sor;

import service.Service;


public class SORServer extends Service implements SORService {

	public void getSolution(double omega, double[][] g, int n_iters) {

		
	//	dist(new SORTask(omega, g, n_iters), new MatrixIndexDist(g.length, g[0].length, SizeOf.Double));
	}
	
}
 